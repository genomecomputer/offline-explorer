"""Selective `.genome` archive reader prototype.

Question: can Offline Explorer fully validate a compressed bundle while storing
only the JSON and Parquet files needed for deterministic local search?
"""
